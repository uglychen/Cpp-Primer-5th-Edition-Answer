/*---------------------------------------------------------------------------

6.39答：（a）顶层const，重复声明；（b）非法，返回类型不一致而参数相同；（c）重载；


6.40答：第二个声明错误；第一个形参提供了默认值，那么后面的形参都必须提供默认值；


6.41答：（a）非法，没有给第一个形参传值；（b）合法；（c）非法，第三个形参必须提供了默认值，那么第二形参不能省略

----------------------------------------------------------------------------*/