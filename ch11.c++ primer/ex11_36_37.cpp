/*-----------------------------------------------------------

11.36答：该什么代替就什么代替，如果一个关键字一个空格，那么用空格代替value。
11.37答：void my_count(unordered_map<string, int> &m)
{
string word;
while (cin >> word )
++m[word];

for (const auto &e : m)
std::cout << e.first << " " << e.second << endl;
}
通常一个无序容器可以替换对应的有序容器，反之亦然。一个无序容器的输出会与使用的有序容器的版本不同。


-------------------------------------------------------------*/