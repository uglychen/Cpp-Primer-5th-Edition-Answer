/*------------------------------------------------------------------------------------


9.21答：第一次插入的时候初始化了iter，把读入的string插入到iter所指的元素之前的位置，其insert返回指向这个新插入元素的迭代器，每读入一次string，重复上述工作。


9.22答：
vector<int>::iterator iter = iv.begin(), mid = iv.begin() + iv.size / 2;
while (iter != mid)
{
if (*iter == some_val)
{
iv.insert(iter, 2 * some_val);
iter += 2;
}
else
++iter;
}


9.23答：肯定是相同的值。





--------------------------------------------------------------------------------------*/