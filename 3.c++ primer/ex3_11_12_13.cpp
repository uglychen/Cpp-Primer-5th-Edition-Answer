3.11答：不合法，s是const string类型字符串，是不允许修改其字符串的内容。

3.12答：b不合法，svec是保存vector<string>类型的对象，而ivec保存的是int型vector的vector，元素类型不相同，所以是不合法的。

3.13答：（a）0个；（b）10个；（c）10个42；（d）一个10；（e）2个元素分别是10,42；（f）10个元素；（g）10个元素都是hi；
